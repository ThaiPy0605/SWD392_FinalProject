# Shared Components Folder

This directory contains shared visual components used across different layouts and pages, such as:
- Button.jsx
- Input.jsx
- Card.jsx
- Navbar.jsx
- Sidebar.jsx

Currently, to optimize execution and keep code consolidated, these elements are directly integrated as sub-components inside `src/layouts` and `src/pages`. They can easily be refactored here as the application scales.
